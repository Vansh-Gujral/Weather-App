Django==5.2.6
requests==2.32.4          # for API calls
python-decouple==3.8      # to read .env
pytz==2023.3              # timezone support
gunicorn==23.0.0          # WSGI server for deployment
whitenoise==6.10.0        # static files serving
beautifulsoup4==4.13.4    # only if parsing HTML is needed (optional)
